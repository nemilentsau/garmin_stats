# 07 — FINDINGS.md Update (extended runs only)

The exact proposed patch. Quote before/after or a diff so review is mechanical.

## New / updated takeaway
> Append under **Analytical Takeaways** with attribution (snapshot date + this run folder).

```
- <takeaway>. (added <snapshot-date>, run <run-folder-name>)
```

## Question transitions
- **Resolved:** move "<question>" from `Open Questions` → `Resolved Questions` with back-reference to this run.
- **Parked (if applicable):** move "<question>" → `Parked Questions` with one-line why.

## Retractions (if this run invalidates a prior takeaway)
```
- ~~<prior takeaway>~~ — retracted <snapshot-date>: <one sentence>, see run <folder>.
```

## Snapshot-section corrections (if numbers drift)
- <section / row>: <old> → <new>, from snapshot bump <date>.
